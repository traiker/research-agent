# Kopieren nach .env und den/die echten Key(s) eintragen:
#   cp .env.example .env
#
# Nur der Key für den Provider wird benötigt, den du per --provider nutzt
# (default: claude). Beide gleichzeitig zu setzen ist unproblematisch.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
